# Prod-like OVH VPS override. All other settings live in ../base/values.yaml.
# Chart 37.4.0 does not render ingressEndpoint.ip as a value, so the public
# address is passed through the supported Traefik static option.
additionalArguments:
  - "--providers.kubernetesingress.ingressendpoint.ip=137.74.174.36"
