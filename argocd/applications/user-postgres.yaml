apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-postgres
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/nicolasdemol/belovr-cluster.git'
    targetRevision: main
    path: helm/databases/user-postgres
    helm:
      releaseName: user-postgres
  destination:
    server: https://kubernetes.default.svc
    namespace: belovr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
